import { FunctionalComponent } from 'preact';

import Chart from "./Chart";

const App: FunctionalComponent = () => {
    return (
        <div id="preact_root">
            <Chart />
        </div>
    );
};

export default App;
